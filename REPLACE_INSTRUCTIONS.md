# AI Agent v5 суулгах

> HISTORICAL ONLY — Do not replace the current project or any whole folder. Use the incremental workflow in `README.md` and `AGENTS.md`.

1. Одоогийн project-оо backup хийнэ.
2. ZIP доторх source файлуудыг project root дээр replace хийнэ.
3. Дараах командыг ажиллуулна:

```bash
npm install
npm run dev
```

4. Шалгах хуудас:
- `/` — AI Agent
- `/docs` — Docs (гарын авлага)
- `/guide` — Docs-ийн нөөц холбоос

5. Deploy хийхийн өмнө:

```bash
npm run build
```
